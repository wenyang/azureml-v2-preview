# yaml-language-server: $schema=https://azuremlsdk2.blob.core.windows.net/latest/environment.schema.json
name: r-with-optparse
version: 1
docker:
   build:
     dockerfile: ../environments/r/r_with_cran.dockerfile
