# template-swagger-setup.yml
# Dynamically generate the _restclient folders

steps:
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      # Setup SDK
      python $(ScriptPath)\dev_setup.py -sdk
